import { Flight } from 'src/lib/entities';

export function solve(flight: Flight): Flight | undefined {
  // Create 1 - n groups
  // Add 1 - n (groups -1) cars to each group
  // Add a person as operator
  // Add 0 - m passengers
  return undefined;
}
